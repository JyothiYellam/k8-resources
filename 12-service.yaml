apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector: 
    project: roboshop
    component: frontend
    environment: dev
  port: 
   - protocol: TCP
     port: 80
     targetport: 80
     
      
      